# AutoML Studio — dependencias
# Local:            pip install -r requirements-full.txt   (incluye CatBoost)
# Streamlit Cloud:  este archivo, que es el que lee la nube automáticamente

# --- Núcleo ---
streamlit>=1.40
pandas>=2.1
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4

# --- Modelos de gradient boosting ---
xgboost>=2.0
lightgbm>=4.0
# catboost>=1.2         # ver requirements-full.txt (pesado para la nube)

# --- Interpretabilidad ---
shap>=0.44

# --- Visualización ---
plotly>=5.18

# --- Lectura/escritura de archivos ---
openpyxl>=3.1          # .xlsx
XlsxWriter>=3.1        # escritura alternativa de Excel
pyarrow>=14.0          # .parquet
joblib>=1.3            # exportar el modelo entrenado
tabulate>=0.9          # reporte en Markdown

# --- Opcionales (mejoran la app, no son obligatorias) ---
# umap-learn>=0.5      # proyección UMAP en la pestaña de Clusters
# odfpy>=1.4           # archivos .ods
# pyxlsb>=1.0          # archivos .xlsb
